const Header = () => {
    return(
        <div className="nav">
            <a href="#">Home</a>
            <a href="#">About</a>
            <a href="#">Contacts</a>
        </div>
    );
}

export default Header